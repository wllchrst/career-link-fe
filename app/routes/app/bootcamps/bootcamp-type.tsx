export const loader = async () => {
  // const { data: bootcampTypes } = await getBootcampTypes();
  // return { bootcampTypes };
};

const BootcampType = () => {
  return (
    <div>
      <h1>Bootcamp Type</h1>
      <p>This is the Bootcamp Type page.</p>
    </div>
  );
};
